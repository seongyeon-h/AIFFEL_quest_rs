# AIFFEL Campus Online Code Peer Review Templete
- 코더 : 황성연
- 리뷰어 : 송세미


# PRT(Peer Review Template)
- [X]  **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**
    - DeepLabV3 모델을 활용하여 사과 고양이 사진에 대해 각각 세그멘테이션 마스크를 생성하고, 배경 블러 처리를 성공적으로 수행했음. 최종 결과 이미지가 노트북 상에 명확히 출력되었다.
    - plt.imshow를 통해 원본, 마스크, 결과물을 단계별로 시각화해서 프로세스를 완벽히 보여주었음.
    
- [X]  **2. 전체 코드에서 가장 핵심적이거나 가장 복잡하고 이해하기 어려운 부분에 작성된 
주석 또는 doc string을 보고 해당 코드가 잘 이해되었나요?**
    - 모델 예측 결과인 output['out'][0]에서 argmax(0)를 추출하여 클래스를 분류하는 핵심 로직에 주석이 상세히 달려있음.
      seg_map = output.argmax(0).byte().cpu().numpy() 부분에서 각 클래스 번호(사람: 15)를 어떻게 마스크로 변환하는지 단계별 설명이 되어 있어 이해하기 쉬웠다.
- [X]  **3. 에러가 난 부분을 디버깅하여 문제를 해결한 기록을 남겼거나
새로운 시도 또는 추가 실험을 수행해봤나요?**
    - 단순 인물을 넘어 고양이(Animal) 클래스에 대한 추가 실험 분석하였음.
      고양이의 수염이나 귀 부분에서 발생하는 세그멘테이션 오류(경계선 뭉개짐)를 직접 확인하고, 이걸 한계점으로 기록 하였다.
        
- [X]  **4. 회고를 잘 작성했나요?**
    - 프로젝트 마무리 부분에 본인이 느낀 기술적 한계(머리카락 경계 처리 등)와 해결 방안에 대한 고민이 잘 기록되어있음.
      단순 구현에 그치지 않고 "왜 이 부분은 깔끔하게 따지지 않을까?"라는 의문을 가지고 모델의 특성을 분석한 점이 매우 훌륭하.
        
- [X]  **5. 코드가 간결하고 효율적인가요?**
    - 전체적으로 변수명이 직관적이며(img_orig, img_mask, img_show 등), 각 단계(이미지 로드 -> 세그멘테이션 -> 합성)가 논리적인 순서로 배치되어 있어 가독성이 높음.
      - 파이썬의 표준 스타일 가이드를 잘 따르고 있으며, 들여쓰기와 연산자 주변의 공백 활용이 적절하여 코드가 깔끔함.
      - 모델의 출력을 처리할 때 argmax와 byte 변환을 연달아 처리하여 불필요한 메모리 낭비를 줄였음.
      - 개선 제안: 현재는 이미지별로 코드가 나열되어 있는데, 이를 함수화(def process_portrait(image_path):)했다면 중복 코드를 더 획기적으로 줄여 범용성을 확보할 수 있을 것 같습니다.


# 회고(참고 링크 및 코드 개선)
```
전체적인 코드 흐름이 노드 예제에 충실하면서도 시각화 perspective으로서 깔끔했습니다. 특히 plt.subplot 등을 활용해 비포/애프터를 한눈에 보여준 점이 좋았습니다.

몇가지 기술적으로 제안드리자면,

1. 함수화(Modularization): 현재는 각 단계가 순차적 셀로 구성되어 있는데, 마스크 생성부터 합성까지를 하나의 함수로 정의했다면 다양한 사진을 더 빠르게 테스트해 볼 수 있었을 것 같습니다.

2. 알파 블렌딩(Alpha Blending): 현재는 np.where를 사용해 경계가 다소 딱딱하게 끊기는데, 마스크에 가우시안 블러를 살짝 주거나 0~1 사이의 alpha 값을 곱해 합성하는 방식을 써보시면 경계면이 훨씬 부드러워질 것이고,

3. Instance Segmentation: 다음 단계로는 여러 명의 사람이 있을 때 각각을 구분할 수 있는 Mask R-CNN 같은 모델을 살펴보시면 이번 프로젝트의 한계점을 최종적으로 해결할 수 있지 않을까 합니다.

전반적으로 프로젝트의 목적을 명확히 이해하고 결과물을 잘 만드셨고, 제가 위에 말씀드린 함수화 구조나 정교한 합성 수식을 다음 프로젝트에 적용해 보신다면 훨씬 더 정교한 이미지 처리 파이프라인을 만드실 수 있을 것 같아요. 수고 많으셨습니다!
```
